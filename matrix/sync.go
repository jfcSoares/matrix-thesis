package matrix

/*import (
	"maunium.net/go/mautrix"
	"maunium.net/go/mautrix/event"
)*/

/*type Syncer struct {
	rooms             *rooms.RoomCache
	globalListeners   []mautrix.SyncHandler
	listeners         map[event.Type][]mautrix.EventHandler // maps an event type to its listeners
	FirstSyncDone     bool
	InitDoneCallback  func()
	FirstDoneCallback func()
	//Progress          ifc.SyncingModal
}

// NewGomuksSyncer returns an instantiated GomuksSyncer
func NewSyncer(rooms *rooms.RoomCache) *Syncer {
	return &Syncer{
		rooms:           rooms,
		globalListeners: []mautrix.SyncHandler{},
		listeners:       make(map[event.Type][]mautrix.EventHandler),
		FirstSyncDone:   false,
	}
}*/

//This is if a custom syncer is required later, for now, the default one will be useds
